#!/bin/sh
sudo pacman -Sy --noconfirm tmux &&
tmux new-session -d -s install &&
tmux send-keys -t install "curl https://raw.githubusercontent.com/voltamage/dotfiles/main/install | sh" &&
tmux attach -t install
